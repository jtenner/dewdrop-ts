pub enum Either<t, u> {
  Left(t),
  Right(u)
}

impl Map<r> for Either<l, r> {
  fn map<u>(cb: (r) => u): Either<l, u> {
    match self {
      Left(val) => Left(val),
      Right(val) => Right(cb(val))
    }
  }
}