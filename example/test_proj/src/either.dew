pub enum Either<t, u> {
  Left(t),
  Right(u)
}

impl Map<r, u> for Either<l, r> {
  fn map(cb: (r) => u): Either<l, u> {
    match self {
      Left(val) => Left(val),
      Right(val) => Right(cb(val))
    }
  }
}